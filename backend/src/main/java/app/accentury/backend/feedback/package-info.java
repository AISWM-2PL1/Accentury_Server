/**
 * 결과 화면의 이용 후기 (KAN-211, 2026-09-15 결정).
 * <p>
 * 응시자가 결과를 본 뒤 개발팀에 보내는 서술 후기다. 로그인이 없으므로 후기는 세션에 매달고
 * 본인 확인도 세션 토큰(Bearer)이며, 세션당 한 번만 받는다. 별점과 회신용 이메일은 선택이다.
 * <p>
 * 후기를 읽는 곳은 슬랙이지만 전송기는 2단계다 - 이 패키지는 저장까지만 한다.
 * 세션과 결과가 24시간 뒤 파기돼도(§5.5) 후기는 1년 남으므로, 읽을 때 맥락이 되는 값
 * (등급, 테스트/점수 버전, 플랫폼, 트래픽 구분)은 저장 시점에 복사해 둔다 -
 * 그래서 {@code session_feedback}은 세션 테이블과 FK로 묶이지 않는다.
 */
@NullMarked
package app.accentury.backend.feedback;

import org.jspecify.annotations.NullMarked;
